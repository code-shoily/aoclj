(ns aoclj.year-2024.solver
  (:require [aoclj.utils :as utils]))

(defn solve [day]
  (let [_ (utils/read-input-data 2024 day)]
    (case day
      ;; Add cases for days as needed
      (str "No solution implemented for Day " day))))